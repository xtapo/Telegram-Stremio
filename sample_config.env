# Telegram (required)
API_ID=""
API_HASH=""
BOT_TOKEN=""
MULTI_TOKENS="" # Optional: comma-separated extra bot tokens for load balancing (e.g. "tok1,tok2,tok3")
OWNER_ID=""

# Database (required — comma-separated URIs, exactly 2: tracking, storage_1)
DATABASE=""

# Server (required)
PORT="8000"
